`szs-patched`, `brres-patched` and `brres-sys-patched` folders are all patched versions of Rii*efi's crates, they had some bugs (a lot) <br/>

and yes i know this is something better done in-game but i aint at that level yet<br/>

todo:
- checkpoints
- obj kcl
- `create` command
- divide the logic in a standalone library crate, then build a cli on top of it
- following the point above, a GUI (less useful but still might be nice)
- rust version of `szs` and `brres` crates (`brres` is MUCH more complicated, probably will never try it)