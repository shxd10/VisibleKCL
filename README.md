mostly needed for TAS comps<br/>
`vmkw kcl --help`<br/>

`szs-patched`, `brres-patched` and `brres-sys-patched` folders are all patched versions of Ri*defi's crates, they had some bugs (a lot) <br/>

and yes i know this is something better done in-game but i aint at that level yet<br/>

todo:
- obj kcl
- `create` command
- divide the logic in a standalone library crate, then build a cli (and maybe GUI if i really want to) on top of it
- rust version of `szs` and `brres` crates (`brres` is MUCH more complicated, probably will never try it)