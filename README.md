`szs-patched`, `brres-patched` and `brres-sys-patched` folders are all patched versions of Rii*efi's crates <br/>
they had some bugs (a lot) <br/>
todo:
- normals
- checkpoints
- colors for trick surfaces etc
- `create` command
- rust version of `szs` and `brres` crates